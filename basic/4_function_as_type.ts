const addNum = (num1: number, num2: number): number => {
  return num1 + num2;
};

const printResult = (num: number): void => {
  console.log(num);
};

let myFunction: () => void;

// next: tsc compiler options
